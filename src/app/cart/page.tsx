"use client";

import { CartPanel } from "@/components/cart/CartPanel";

export default function CartPage() {
  return (
    <div className="cart-page-fullscreen">
      <CartPanel variant="page" />
    </div>
  );
}
